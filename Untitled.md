# جلسه ۳ - بخش ۳: همبستگی و رگرسیون خطی در پژوهش‌های آبزی‌پروری

🎯 **هدف بخش:** در این بخش پژوهشگر یاد می‌گیرد چگونه روابط بین متغیرهای کمی را در داده‌های واقعی آبزی‌پروری بررسی کند؛ ابتدا با **همبستگی** شدت و جهت رابطه را بسنجد و سپس با **رگرسیون خطی ساده و چندگانه** مقدار یک متغیر را بر اساس یک یا چند متغیر دیگر پیش‌بینی کند.

---

# تفاوت همبستگی و رگرسیون

|مفهوم|هدف|خروجی اصلی|مثال در آبزی‌پروری|
|---|---|---|---|
|همبستگی|بررسی شدت و جهت رابطه|ضریب همبستگی و p-value|رابطه بین زیست‌توده و خوراک روزانه|
|رگرسیون خطی ساده|پیش‌بینی پاسخ با یک متغیر|معادله خط، ضرایب، R²|پیش‌بینی وزن میگو بر اساس روز پرورش|
|رگرسیون خطی چندگانه|پیش‌بینی پاسخ با چند متغیر|اثر همزمان عوامل مختلف|پیش‌بینی زیست‌توده استخر بر اساس خوراک، تراکم و اکسیژن|

📌 **نکته مهم:** همبستگی فقط وجود رابطه را بررسی می‌کند، اما رگرسیون یک مدل پیش‌بینی و تفسیر اثر ارائه می‌دهد.

---

# مثال ۱: منحنی رشد میگو و پیش‌بینی وزن بر اساس زمان پرورش

## مسئله پژوهشی

در یک استخر پرورش میگو، هر هفته زیست‌سنجی انجام شده است. در هر نوبت حدود ۵۰ قطعه میگو نمونه‌برداری و میانگین وزن آن‌ها ثبت شده است.

هدف این است که بررسی کنیم:

> آیا می‌توان وزن متوسط میگو را از روی روز پرورش پیش‌بینی کرد؟

---

## بارگذاری داده

```r
growth <- read.csv("dataset/shrimp_growth_curve.csv")

head(growth)
```

```
  week doc abw_g n_sampled
1    1   7  1.05        52
2    2  14  1.51        48
3    3  21  3.64        52
4    4  28  5.06        52
5    5  35  5.00        49
6    6  42  6.76        51
```

ساختار داده:

|متغیر|توضیح|
|---|---|
|week|هفته پرورش|
|doc|روز پرورش|
|abw_g|میانگین وزن میگو (گرم)|
|n_sampled|تعداد نمونه|

---

# نمودار پراکنش

```r
plot(growth$doc,
     growth$abw_g,
     pch = 19,
     col = "blue",
     xlab = "Days of Culture",
     ylab = "Average Body Weight (g)")
```

<img src="img/Pasted image 20260824201641.png"  width="600" >


📌 سؤال:

آیا رابطه واقعاً خطی است؟

معمولاً پژوهشگر در نگاه اول تصور می‌کند که پاسخ مثبت است.

---

# همبستگی پیرسون

```r
cor.test(growth$doc,
         growth$abw_g,
         method = "pearson")
```

```
Pearson's product-moment correlation

data:  growth$doc and growth$abw_g
t = 48.771, df = 15, p-value < 2.2e-16
alternative hypothesis: true correlation is not equal to 0
95 percent confidence interval:
 0.9910788 0.9988981
sample estimates:
      cor 
0.9968617
```
### تفسیر

اگر ضریب همبستگی بالا و مثبت باشد:

> هرچه روز پرورش افزایش می‌یابد، وزن میگو نیز افزایش می‌یابد.

---

# رگرسیون خطی ساده

```r
model_growth <- lm(abw_g ~ doc,
                   data = growth)

summary(model_growth)
```

```
Call:
lm(formula = abw_g ~ doc, data = growth)

Residuals:
     Min       1Q   Median       3Q      Max 
-1.10167 -0.40765  0.09282  0.51983  1.19431 

Coefficients:
             Estimate Std. Error t value Pr(>|t|)    
(Intercept) -1.705809   0.328078  -5.199 0.000108 ***
doc          0.223071   0.004574  48.771  < 2e-16 ***
---
Signif. codes:  
0 ‘***’ 0.001 ‘**’ 0.01 ‘*’ 0.05 ‘.’ 0.1 ‘ ’ 1

Residual standard error: 0.6467 on 15 degrees of freedom
Multiple R-squared:  0.9937,	Adjusted R-squared:  0.9933 
F-statistic:  2379 on 1 and 15 DF,  p-value: < 2.2e-16
```

مدل:

$$  
ABW = \beta_0 + \beta_1 DOC  
$$

---

# بررسی باقی‌مانده‌ها

```r
plot(model_growth)
```

📌 در این مثال نکته مهم آموزشی وجود دارد:

اگر نمودار **Residuals vs Fitted** را بررسی کنیم، معمولاً الگوی خمیده مشاهده می‌شود.

این نشان می‌دهد که رشد میگو کاملاً خطی نیست.

---

# تبدیل لگاریتمی

در بسیاری از مزارع، رشد میگو تقریباً از یک رابطه توانی پیروی می‌کند:

$$  
ABW = a \times DOC^b  
$$

لگاریتم گرفتن از دو طرف:

$$  
\log(ABW)=\log(a)+b\log(DOC)  
$$

در R:

```r
model_log <- lm(log(abw_g) ~ log(doc),
                data = growth)

summary(model_log)
```

```
Call:
lm(formula = log(abw_g) ~ log(doc), data = growth)

Residuals:
     Min       1Q   Median       3Q      Max 
-0.28221 -0.01053  0.01493  0.03586  0.17373 

Coefficients:
            Estimate Std. Error t value Pr(>|t|)    
(Intercept)  -2.4249     0.1357  -17.86 1.61e-11 ***
log(doc)      1.1819     0.0340   34.76 9.39e-16 ***
---
Signif. codes:  0 ‘***’ 0.001 ‘**’ 0.01 ‘*’ 0.05 ‘.’ 0.1 ‘ ’ 1

Residual standard error: 0.1082 on 15 degrees of freedom
Multiple R-squared:  0.9877,	Adjusted R-squared:  0.9869 
F-statistic:  1208 on 1 and 15 DF,  p-value: 9.393e-16
```
📌 > ضریب تعیین بالا به تنهایی کافی نیست و همیشه باید نمودار باقی‌مانده‌ها نیز بررسی شود.

---

# مثال ۲: همبستگی بین زیست‌توده و خوراک روزانه

## مسئله پژوهشی

خوراک بزرگ‌ترین هزینه تولید میگو است.

مدیر مزرعه می‌خواهد بداند:

> آیا خوراک روزانه با زیست‌توده موجود در استخر رابطه دارد؟

---

## بارگذاری داده

```r
feed <- read.csv("daily_feed.csv")

head(feed)
```

|متغیر|توضیح|
|---|---|
|biomass_kg|زیست‌توده تخمینی|
|water_temp_c|دمای آب|
|daily_feed_kg|خوراک روزانه|

---

# نمودار پراکنش

```r
plot(feed$biomass_kg,
     feed$daily_feed_kg,
     pch = 19,
     col = "darkgreen",
     xlab = "Biomass (kg)",
     ylab = "Daily Feed (kg)")
```

---

# همبستگی پیرسون

```r
cor.test(feed$biomass_kg,
         feed$daily_feed_kg)
```

### تفسیر

اگر:

```text
r = 0.85
```

باشد:

> هرچه زیست‌توده افزایش یابد، مقدار خوراک مصرفی نیز افزایش می‌یابد.

---

# تفسیر شدت همبستگی

|مقدار r|تفسیر|
|---|---|
|0 تا 0.3|ضعیف|
|0.3 تا 0.5|متوسط|
|0.5 تا 0.7|نسبتاً قوی|
|بالاتر از 0.7|قوی|

📌 در پژوهش‌های کاربردی آبزی‌پروری علاوه بر p-value باید اندازه اثر نیز تفسیر شود.

---

# همبستگی اسپیرمن

اگر داده‌ها نرمال نباشند:

```r
cor.test(feed$biomass_kg,
         feed$daily_feed_kg,
         method="spearman")
```

---

# ماتریس همبستگی

```r
cor(feed[,c("doc",
            "biomass_kg",
            "water_temp_c",
            "daily_feed_kg")])
```

---

# نمودار ماتریس پراکنش

```r
pairs(feed[,c("doc",
              "biomass_kg",
              "water_temp_c",
              "daily_feed_kg")])
```

این نمودار امکان بررسی اولیه روابط بین متغیرها را فراهم می‌کند.

---

# مثال ۳: پیش‌بینی خوراک روزانه با رگرسیون چندگانه

## مسئله پژوهشی

مدیر مزرعه می‌خواهد قبل از خوراک‌دهی روزانه مقدار خوراک موردنیاز را تخمین بزند.

دو متغیر در دسترس:

- زیست‌توده استخر
    
- دمای آب
    

---

# مدل رگرسیون چندگانه

```r
model_feed <- lm(
  daily_feed_kg ~ biomass_kg + water_temp_c,
  data = feed
)

summary(model_feed)
```

مدل:

$$  
Feed =  
\beta_0 +  
\beta_1 Biomass +  
\beta_2 Temperature  
$$

---

# تفسیر ضرایب

فرض کنید خروجی چنین باشد:

```text
biomass_kg      0.022
water_temp_c    1.15
```

### تفسیر ضریب زیست‌توده

به ازای هر یک کیلوگرم افزایش زیست‌توده:

```text
0.022 کیلوگرم
```

خوراک روزانه افزایش می‌یابد.

یا به بیان مدیریتی:

> به ازای هر 100 کیلوگرم افزایش زیست‌توده، حدود 2.2 کیلوگرم خوراک بیشتری نیاز است.

---

### تفسیر ضریب دما

با ثابت بودن زیست‌توده:

> افزایش یک درجه سانتی‌گراد دما، مصرف خوراک را حدود 1.15 کیلوگرم افزایش می‌دهد.

این تفسیر برای مدیر مزرعه بسیار ملموس و کاربردی است.

---

# پیش‌بینی

فرض کنید:

```text
Biomass = 700 kg
Temperature = 30°C
```

```r
newdata <- data.frame(
  biomass_kg = 700,
  water_temp_c = 30
)

predict(model_feed,
        newdata = newdata)
```

---

# بررسی مفروضات رگرسیون

```r
plot(model_feed)
```

موارد بررسی:

- خطی بودن
    
- نرمال بودن باقی‌مانده‌ها
    
- همگنی واریانس
    
- نقاط پرت
    

---

# آزمون نرمال بودن باقی‌مانده‌ها

```r
shapiro.test(
 residuals(model_feed)
)
```

---

# مثال ۴: پیش‌بینی زیست‌توده برداشت با رگرسیون چندگانه

## مسئله پژوهشی

در پایان دوره پرورش، مدیر مزرعه می‌خواهد قبل از صید زیست‌توده قابل برداشت را تخمین بزند.

داده‌های ۴۰ استخر در اختیار داریم.

---

## بارگذاری داده

```r
pond <- read.csv("pond_biomass.csv")
```

---

## متغیرها

|متغیر|توضیح|
|---|---|
|density_pm2|تراکم ذخیره‌سازی|
|doc|طول دوره پرورش|
|mean_do_mgL|میانگین اکسیژن محلول|
|total_feed_kg|مجموع خوراک مصرفی|
|biomass_kg|زیست‌توده برداشت‌شده|

---

# رگرسیون چندگانه

```r
model_biomass <- lm(
  biomass_kg ~ density_pm2 +
               doc +
               mean_do_mgL +
               total_feed_kg,
  data = pond
)

summary(model_biomass)
```

---

# تفسیر مدیریتی

فرض کنید ضرایب به شکل زیر باشند:

```text
density_pm2      15
doc              10
mean_do_mgL     180
total_feed_kg   0.55
```

### تراکم

افزایش یک قطعه در مترمربع:

```text
15 kg
```

زیست‌توده برداشت را افزایش می‌دهد.

---

### اکسیژن محلول

افزایش یک میلی‌گرم بر لیتر اکسیژن:

```text
180 kg
```

به زیست‌توده نهایی اضافه می‌کند.

---

### خوراک

هر یک کیلوگرم خوراک اضافی:

```text
0.55 kg
```

به زیست‌توده برداشت کمک می‌کند.

---

# یک نکته بسیار مهم پژوهشی: نشت اطلاعات (Data Leakage)

در این فایل دو متغیر نیز وجود دارند:

```text
survival_pct
abw_g
```

اگر این دو متغیر را وارد مدل کنیم:

```r
lm(biomass_kg ~ survival_pct + abw_g + ...)
```

ممکن است R² بسیار بزرگ شود.

اما این مدل ارزش مدیریتی ندارد.

چرا؟

زیرا:

# $$  
Biomass

N_0  
\times Survival  
\times ABW  
$$

بنابراین این متغیرها عملاً بخشی از خود زیست‌توده هستند.

📌 این یکی از رایج‌ترین خطاهای تحلیل داده در پروژه‌های آبزی‌پروری است.

---

# بررسی هم‌خطی (Multicollinearity)

در این مثال خوراک و تراکم تا حدی با یکدیگر همبسته‌اند.

برای بررسی:

```r
library(car)

vif(model_biomass)
```

قاعده تجربی:

|VIF|تفسیر|
|---|---|
|کمتر از 5|قابل قبول|
|5 تا 10|هشدار|
|بیشتر از 10|هم‌خطی شدید|

---

# مقایسه مدل‌ها

```r
model1 <- lm(
 biomass_kg ~ density_pm2 + doc,
 data = pond
)

model2 <- lm(
 biomass_kg ~ density_pm2 +
              doc +
              mean_do_mgL +
              total_feed_kg,
 data = pond
)

anova(model1, model2)
```

---

# گزارش نتایج همبستگی

نمونه گزارش:

> بین زیست‌توده استخر و خوراک روزانه رابطه مثبت و معنی‌داری مشاهده شد (Pearson correlation, r = 0.84, p < 0.001). بنابراین با افزایش زیست‌توده موجود در استخر، میزان خوراک مصرفی نیز افزایش یافت.

---

# گزارش نتایج رگرسیون

نمونه گزارش:

> مدل رگرسیون چندگانه برای پیش‌بینی زیست‌توده برداشت‌شده معنی‌دار بود (F-test, p < 0.001). مقدار R² تعدیل‌شده نشان داد که بخش عمده‌ای از تغییرات زیست‌توده توسط تراکم ذخیره‌سازی، طول دوره پرورش، اکسیژن محلول و خوراک مصرفی توضیح داده می‌شود. در میان متغیرهای بررسی‌شده، خوراک مصرفی و اکسیژن محلول بیشترین اثر مثبت را بر زیست‌توده نهایی داشتند.

---

## جمع‌بندی آموزشی این بخش

در این بخش چهار سناریوی واقعی آبزی‌پروری بررسی شد:

1. **رشد میگو بر حسب زمان پرورش** → همبستگی، رگرسیون ساده و تبدیل لگاریتمی
    
2. **رابطه زیست‌توده و خوراک روزانه** → همبستگی پیرسون و اسپیرمن
    
3. **پیش‌بینی خوراک روزانه** → رگرسیون چندگانه و تفسیر ضرایب
    
4. **پیش‌بینی زیست‌توده برداشت** → رگرسیون چندگانه، مقایسه مدل‌ها، هم‌خطی و Data Leakage
    

این چهار مثال تقریباً تمام مفاهیم کلیدی همبستگی و رگرسیون موردنیاز یک پژوهشگر حوزه میگو و آبزی‌پروری را پوشش می‌دهند.
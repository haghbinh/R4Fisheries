
# مثال ۱: منحنی رشد و پیش‌بینی وزن برحسب زمان پرورش

هدف مطالعه
تعیین الگوی افزایش مییش‌بینی وزن برحسب زمان پرورش

هدف مطالعه
تعیین الگوی افزایش میزن در روزهای آینده. این پیش‌بینی در عمل مبنای برنامه‌ریزی خوراک، زمان‌بندی صید و برآورد تاریخ رسیدن به وزن بازاری است.

شیوه گردآوری داده
یک استخر خاکی پرورش میگو در طول یک دوره کامل پایش شده است. از هفته اول پس از ذخیره‌سازی، هر هفته یک نوبت زیست‌سنجی انجام می‌شود: با تور پرتابی از چند نقطه استخر نمونه‌برداری می‌شود، حدود ۵۰ قطعه میگو به‌صورت زنده توزین و پس از ثبت داده به استخر بازگردانده می‌شوند. میانگین وزن همان ۵۰ قطعه به‌عنوان مقدار ثبت‌شده آن نوبت گزارش می‌شود. بنابراین واحد مشاهده «یک نوبت زیست‌سنجی» است، نه «یک میگو».

ساختار داده (`shrimp_growth_curve.csv`)

| ستون | توضیح | واحد | دامنه تقریبی |
|---|---|---|---|
| `week` | شماره هفته پرورش | هفته | ۱ تا ۱۷ |
| `doc` | روز پرورش (Days of Culture) | روز | ۷ تا ۱۱۹ |
| `abw_g` | میانگین وزن بدن نمونه‌های توزین‌شده | گرم | حدود ۰٫۵ تا ۳۰ |
| `n_sampled` | تعداد قطعات توزین‌شده در آن نوبت | قطعه | حدود ۵۰ |

نکته آموزشی
رشد میگو در این بازه تقریباً توانی است، پس برازش رگرسیون خطی ساده روی مقادیر خام، ضریب تعیین بالایی می‌دهد اما باقی‌مانده‌ها الگوی منحنی‌دار نشان خواهند داد. همین موضوع بهانه طبیعی ورود به تبدیل لگاریتمی و مقایسه دو مدل است.

# مثال ۲: پیش‌بینی زیست‌توده کل استخر در پایان دوره

هدف مطالعه
شناسایی عوامل مدیریتی و محیطی مؤثر بر زیست‌توده برداشت‌شده از هر استخر و ساخت یک مدل رگرسیون چندگانه برای برآورد زیست‌توده پیش از صید. تصمیم عملی مرتبط با آن، انتخاب تراکم ذخیره‌سازی و برنامه خوراک‌دهی است.

شیوه گردآوری داده
داده‌های پایان دوره ۴۰ استخر از یک مجتمع پرورشی در یک فصل تولید گردآوری شده است. برای هر استخر مساحت، تعداد و تراکم ذخیره‌سازی، طول دوره پرورش، مجموع خوراک مصرف‌شده در کل دوره، میانگین دوره‌ای اکسیژن محلول (حاصل از پایش روزانه، نه یک اندازه‌گیری لحظه‌ای)، نرخ بقای برآوردی و میانگین وزن نهایی ثبت شده است. واحد مشاهده «یک استخر در یک دوره» است.

ساختار داده (`pond_biomass.csv`)

| ستون | توضیح | واحد | دامنه تقریبی |
|---|---|---|---|
| `pond_id` | شناسه استخر | — | ۱ تا ۴۰ |
| `area_m2` | مساحت استخر | مترمربع | ۳۰۰۰ تا ۵۰۰۰ |
| `density_pm2` | تراکم ذخیره‌سازی | قطعه بر مترمربع | ۲۰ تا ۸۰ |
| `doc` | طول دوره پرورش تا صید | روز | ۹۵ تا ۱۳۰ |
| `mean_do_mgL` | میانگین دوره‌ای اکسیژن محلول | میلی‌گرم بر لیتر | حدود ۳٫۵ تا ۶٫۵ |
| `survival_pct` | نرخ بقا | درصد | حدود ۵۰ تا ۹۰ |
| `abw_g` | میانگین وزن نهایی | گرم | حدود ۱۵ تا ۲۵ |
| `total_feed_kg` | مجموع خوراک مصرفی دوره | کیلوگرم | چند صد تا چند هزار |
| `biomass_kg` | زیست‌توده برداشت‌شده | کیلوگرم | حدود ۵۰۰ تا ۵۰۰۰ |

نکته آموزشی
رابطه ساختاری زیست‌توده به‌صورت $\text{Biomass} = N_0 \times S \times \text{ABW}$ است، یعنی اگر `survival_pct` و `abw_g` هم‌زمان به‌عنوان پیش‌بین وارد مدل شوند، مدل به یک اتحاد حسابداری تبدیل می‌شود و نشت اطلاعات رخ می‌دهد. مدل مفید، مدلی است که فقط از متغیرهای در دسترس پیش از صید (تراکم، روز پرورش، خوراک، اکسیژن) استفاده کند. افزایش تراکم زیست‌توده کل را بالا می‌برد ولی میانگین وزن و بقا را کاهش می‌دهد؛ همین تضاد، بستر مناسبی برای بحث هم‌خطی و ضریب تعیین تعدیل‌شده است.

# مثال ۳: پیش‌بینی مصرف خوراک روزانه

هدف مطالعه
برآورد مقدار خوراک روزانه یک استخر برحسب زیست‌توده موجود و دمای آب، با هدف کاهش خوراک‌دهی مازاد. خوراک بزرگ‌ترین جزء هزینه در پرورش میگو است و خوراک اضافی هم هزینه است و هم کیفیت آب را تخریب می‌کند.

شیوه گردآوری داده
۳۵ رکورد «استخر–روز» از دفتر ثبت روزانه مزرعه استخراج شده است. برای هر رکورد، زیست‌توده تخمینی استخر در آن روز (برآمده از آخرین زیست‌سنجی و نرخ بقای فرضی)، میانگین دمای آب آن روز، روز پرورش و مقدار خوراک واقعاً توزیع‌شده ثبت شده است. واحد مشاهده «یک روز از یک استخر» است.

ساختار داده (`daily_feed.csv`)

| ستون | توضیح | واحد | دامنه تقریبی |
|---|---|---|---|
| `pond_id` | شناسه استخر | — | چند استخر |
| `doc` | روز پرورش | روز | ۴۵ تا ۱۱۵ |
| `biomass_kg` | زیست‌توده تخمینی همان روز | کیلوگرم | ۱۵۰ تا ۱۱۰۰ |
| `water_temp_c` | میانگین دمای آب | درجه سلسیوس | ۲۶ تا ۳۳ |
| `daily_feed_kg` | خوراک توزیع‌شده در آن روز | کیلوگرم | حدود ۳ تا ۳۵ |

نکته آموزشی
خوراک روزانه در عمل درصدی از زیست‌توده است و دما اشتها را تعدیل می‌کند؛ پس ضریب شیب تفسیر مستقیم و ملموسی دارد: کیلوگرم خوراک به‌ازای هر کیلوگرم زیست‌توده. این مثال برای تمرین تفسیر ضرایب و واحدها مناسب‌تر از تمرین انتخاب متغیر است.

# مثال ۴: پیش‌بینی نرخ بقا از شاخص‌های کیفیت آب

هدف مطالعه
سنجش این‌که چه مقدار از تغییرپذیری نرخ بقا میان استخرها با شاخص‌های اکسیژن محلول و تراکم ذخیره‌سازی توضیح داده می‌شود. بقا مستقیم‌ترین عامل مؤثر بر سود دوره است و افت آن معمولاً دیر تشخیص داده می‌شود.

شیوه گردآوری داده
۳۵ استخر در پایان دوره بررسی شده‌اند. اکسیژن محلول هر استخر روزانه در سپیده‌دم (کمینه شبانه‌روزی) اندازه‌گیری شده و از آن دو شاخص ساخته شده است: میانگین دوره‌ای اکسیژن سپیده‌دم، و تعداد روزهایی که اکسیژن از آستانه بحرانی پایین‌تر رفته است. نرخ بقا از مقایسه تعداد ذخیره‌سازی با تعداد برآوردی زمان صید محاسبه شده است. واحد مشاهده «یک استخر در یک دوره» است.

ساختار داده (`survival_waterquality.csv`)


| ستون | توضیح | واحد | دامنه تقریبی |
|---|---|---|---|
| `pond_id` | شناسه استخر | — | ۱ تا ۳۵ |
| `density_pm2` | تراکم ذخیره‌سازی | قطعه بر مترمربع | ۲۰ تا ۸۰ |
| `mean_dawn_do_mgL` | میانگین اکسیژن محلول سپیده‌دم | میلی‌گرم بر لیتر | حدود ۳ تا ۶ |
| `days_below_critical` | تعداد روزهای زیر آستانه بحرانی اکسیژن | روز | ۰ تا حدود ۲۵ |
| `survival_pct` | نرخ بقا | درصد | حدود ۴۵ تا ۹۰ |


---
